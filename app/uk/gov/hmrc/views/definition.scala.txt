@import uk.gov.hmrc.controllers.ApiDefinitionConfiguration
@import play.api.libs.json.Json
@(config: ApiDefinitionConfiguration)
{
  "scopes":[],
  "api": {
    "name": "API Scope",
    "description": "Internal API for use by the API Platform",
    "context": "api-scope",
    "versions": [
      @for((version, index) <- config.versions.zipWithIndex) {
      {
        "version": "@version.version",
        "status": "@version.status",
        "endpointsEnabled": @version.endpointsEnabled,
        "access": @Json.toJson(version.access)
      }@if(index < config.versions.size - 1) {,}
      }
    ]
  }
}
